import * as build from "../src/build";
import * as counter from "../src/counter";
import * as entity from "../src/entity";
import * as expr from "../src/expr";
import * as mod from "../src/mod";
import * as stat from "../src/stat";
import * as toggle from "../src/toggle";

export default {
  build: build,
  counter: counter,
  entity: entity,
  expr: expr,
  mod: mod,
  stat: stat,
  toggle: toggle,
};
